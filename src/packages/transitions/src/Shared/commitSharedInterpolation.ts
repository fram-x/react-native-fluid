import {
  SharedInterpolationType,
  AnimationContextType
} from "../Components/Types";

export const commitSharedInterpolation = (
  sharedInterpolation: SharedInterpolationType,
  _animationContext: AnimationContextType
) => {
  // register interpolations
  if (sharedInterpolation.fromStyles && sharedInterpolation.toStyles) {
  }
};
